@{
    ViewBag.Title = "关于";
    Layout = null;
}
@using Y.Core.Web.Expression
@using Y.Core.Web
<ul>
    @{
      foreach (NavTree item in ViewBag.NavList)
      {
        Html.NavTree(item);
      }
      Html.NavTree((NavTree)ViewBag.NavList[0]);
    }
</ul>

